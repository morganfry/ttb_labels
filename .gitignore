node_modules
.next
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts
context/
# Local sample inputs. PDFs and JPGs are versioned via Git LFS (see
# .gitattributes); the bulky archives and the CSV samples stay out of git.
test-pdfs/*.zip
test-jpgs/*.zip
test-csvs/
